:::info

**Using a self-hosted runner?** MobSF runs on the runner itself, so it has to be installed there once before this step can be used. Follow [MobSF Setup on the Runner](/self-hosted-appcircle/self-hosted-runner/configure-runner/mobsf-setup), which also covers keeping the installation in a macOS virtual machine image. Appcircle cloud runners already have MobSF installed, and need no extra setup.

:::
